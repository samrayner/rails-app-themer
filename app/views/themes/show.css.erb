.themed {
  background-color: <%= theme.color(:background) %>;
  color: <%= theme.color(:text) %>;
  font-family: <%= theme.font(:body) %>;
  background-image: <%= theme.image_url(:logo) %>;
}

.themed h1 {
  font-family: <%= theme.font(:headings) %>;
}
